# d. Add unit tests for testing that function correctly graphs various inputs.
CountryPopulation("New Zealand")
CountryPopulation("Morocco")
CountryPopulation("Niger")
# e. Add unit tests to check for other inputs (If the country is not present,
# then the function CountryPopulation should return an error.)
CountryPopulation("Middle Earth")
# returns error
CountryPopulation("New Zeeland")
# returns error
